# Leave unset for local dev (Vite proxies /auth, /events, etc. to localhost:3001).
# In production, set to your backend public URL (no trailing slash):
# VITE_API_URL=https://your-backend.up.railway.app
